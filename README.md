# Path-Finding
A Python program that loads a graph of Adventure Bay, implements the A* algorithm with heuristic optimization, and computes shortest paths between specified locations, writing a complete summary to an output file.
